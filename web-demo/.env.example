# ============================================================
# 部署凭据（可选，但推荐）
# ============================================================
# 什么情况下需要它：
#   - 如果你已经用 `npx wrangler login` 授权成功 —— 不需要这个文件
#   - 如果 OAuth 登录一直失败（比如卡在 "Fetching auth token"）—— 用这个文件绕开
#
# 三步拿到 token：
#   1. 浏览器打开 https://dash.cloudflare.com/profile/api-tokens
#   2. Create Token → 选 "Edit Cloudflare Workers" 模板 → Continue to summary → Create Token
#   3. 复制生成的 token，粘贴到下面等号后面（不加引号、不留空格）
#
# 用法：把本文件复制为 .env（同目录），填好 token 即可。
#       wrangler 会自动读取当前目录下的 .env。
#
# 安全：.env 已在 .gitignore 中，不会被提交。但请仍然不要把它发给任何人。
# ============================================================

CLOUDFLARE_API_TOKEN=
