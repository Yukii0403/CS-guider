# ============================================================
# 本地开发用的环境变量示例
# ============================================================
# 用法：
#   1. 复制本文件为 .dev.vars（同目录）
#   2. 把 MODEL_API_KEY 换成你真实的 DeepSeek Key
#   3. 运行 npm run dev，wrangler 会自动读取 .dev.vars
#
# 重要：.dev.vars 已在 .gitignore 中，绝不会被提交。
#       千万不要把真实 Key 写进这个 .example 文件。
# ============================================================

# 必填：DeepSeek 控制台拿到的 API Key
MODEL_API_KEY=sk-在这里粘贴你的key

# 选填：接口地址。用 DeepSeek 就保持默认
MODEL_BASE_URL=https://api.deepseek.com/v1

# 选填：模型名。deepseek-chat 便宜够用
MODEL_NAME=deepseek-chat

# 选填：允许的页面来源。本地调试用 * 即可
ALLOW_ORIGIN=*
